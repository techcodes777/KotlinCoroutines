package com.eclatsol.kotlincoroutines

data class HelloData(val isActive : String, val isNotActive : String){

//    Differences between data classes and normal classes.
//    A data class must be declared with at least one primary constructor parameter which must be declared with val or var. A normal class can be defined with or without a parameter in its constructor.7 Feb 2024
    //This is data data class
}
